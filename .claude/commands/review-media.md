# Review Media (week ${WEEK})

Creative pass over `media_picks.json`: each pick must earn its slot (does it
land the beat the surrounding prose sets up?), be technically clean (loads,
loops, legible at 480px), and fit the edition's tone. Reject weak picks back
to `/pick-media` with the reason. Approve only a set you would publish.
