# Render the Preseason Edition

Turn approved `content/preseason-2025/preseason_content.json` into
`preseason-2025.html` under the same rules as `/render-week` (site chrome,
everything inline, render what the edition contains, media tokens, browser
verification), and add it to `config.js` `pages` leading the "columns" group.
